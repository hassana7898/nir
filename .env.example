# Production environment variables for Liara
NODE_ENV=production
GEMINI_API_KEY=replace_with_your_gemini_api_key
GEMINI_MODEL=gemini-2.5-flash
# Optional: comma-separated allowed browser origins. Leave empty for permissive CORS.
CORS_ORIGIN=
JSON_BODY_LIMIT=50mb
